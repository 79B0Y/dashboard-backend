# .env.example – 环境变量模板
# -----------------------------------------
# 将此文件复制为 .env 并根据实际环境修改
#   cp .env.example .env
# -----------------------------------------

# MongoDB 连接串（Docker Compose 内使用服务名 mongo）
MONGO_URI=mongodb://localhost:27017/dashboard

# 应用监听端口（server.js & PM2 使用）
PORT=3001

# 其他可选环境变量
# LOG_LEVEL=info
# JWT_SECRET=changeme
