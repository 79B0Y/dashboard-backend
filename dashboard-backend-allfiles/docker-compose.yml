
version: '3.8'
services:
  mongo:
    image: mongo:6
    ports: ["27017:27017"]
    volumes: [mongo-data:/data/db]
  backend:
    build: .
    ports: ["3001:3001"]
    environment:
      - MONGO_URI=mongodb://mongo:27017/dashboard
    depends_on: [mongo]
volumes:
  mongo-data:
